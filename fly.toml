app = "polymarket-notifier"
primary_region = "fra"

[build]

[env]
  POLL_INTERVAL_SECONDS = "30"
  STATE_PATH = "/data/state.json"

[mounts]
  source = "polymarket_notifier_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[processes]
  app = "node src/index.js"
